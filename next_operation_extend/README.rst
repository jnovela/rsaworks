Next Operation Customization
================================================
-Module contains functionality that if all the preceding work orders is not finish then system will raise a warning,else the system will work with the normal flow.